<?xml version="1.0"?>
<ruleset name="report_allylti">
    <description>PHPCS rules for report_allylti</description>

    <!-- Moodle coding standard -->
    <rule ref="moodle"/>

    <!-- What to scan -->
    <file>.</file>

    <!-- What to ignore -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/vendorjs/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/build/*</exclude-pattern>
    <exclude-pattern>*/.moodledata/*</exclude-pattern>
</ruleset>
